# 运行 lint-staged 检查暂存的文件（--quiet 屏蔽"没有匹配到暂存文件"之类的提示，不影响真实报错输出）
npx lint-staged --quiet

# 运行 Rust 格式化检查
cd src-tauri && cargo fmt --check && cd ..

# 运行 Rust clippy 检查（--quiet 隐藏 Compiling/Finished 编译进度，仅显示警告）
cd src-tauri && cargo clippy --quiet -- -D warnings && cd ..
